// @flow
import React from 'react';
import type { PageProps } from './props';

const PageComponent = ({ children }: PageProps) => <React.Fragment>{children}</React.Fragment>;

export default PageComponent;

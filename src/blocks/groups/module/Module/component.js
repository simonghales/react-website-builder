// @flow
import React from 'react';
import type { ModuleProps } from './props';

const ModuleComponent = ({ children }: ModuleProps) => <React.Fragment>{children}</React.Fragment>;

export default ModuleComponent;

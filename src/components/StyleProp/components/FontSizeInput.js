// @flow
import React from 'react';
import Input, { inputStyleTypes } from '../../Input/Input';

const FontSizeInput = () => <Input styleType={inputStyleTypes.dark} value="" onChange={() => {}} />;

export default FontSizeInput;

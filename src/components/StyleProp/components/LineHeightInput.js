// @flow
import React from 'react';
import Input, { inputStyleTypes } from '../../Input/Input';

const LineHeightInput = () => (
  <Input styleType={inputStyleTypes.dark} value="" onChange={() => {}} />
);

export default LineHeightInput;

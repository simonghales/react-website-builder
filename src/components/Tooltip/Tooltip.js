// @flow
import React from 'react';
import ReactTooltip from 'react-tooltip';
import styles from './styles';

const Tooltip = () => <ReactTooltip className={styles.tooltipClass} effect="solid" />;

export default Tooltip;
